name: Dev - CI/CD
on:
  pull_request:

jobs:
  golangci:
    name: tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
      - name: tests
        run: go test ./...
